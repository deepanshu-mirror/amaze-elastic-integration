- version: "1.0.0"
  changes:
    - description: Initial push-only AMaze Elastic package — receive Rich Alerts pushed by the AMaze dispatcher over webhook (dispatch data stream) and syslog (syslog data stream, UDP/TCP)
      type: enhancement
      link: https://github.com/MirrorMire/amaze-elastic-integration/pull/1